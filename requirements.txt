# ─────────────────────────────────────────────────────────────
# Geospatial stack — INSTALL THESE FIRST, IN THIS ORDER.
# On Windows/Python 3.11 they install as prebuilt binary wheels
# (cp311-win_amd64). fiona and rasterio BUNDLE GDAL inside the
# wheel (~23-25 MB each), so a separate GDAL install is NOT needed.
#   pip install shapely==2.0.6 pyproj==3.6.1
#   pip install fiona==1.9.6 rasterio==1.3.11
#   pip install geopandas==0.14.4
# ─────────────────────────────────────────────────────────────
shapely==2.0.6
pyproj==3.6.1
fiona==1.9.6
rasterio==1.3.11
geopandas==0.14.4

# pandas pinned to 2.x: pandas 3.0 is very new and geemap/streamlit
# ecosystems are still catching up. 2.2.3 is the safe stable choice.
pandas==2.2.3

# ─────────────────────────────────────────────────────────────
# Pure-Python app packages (plain `pip install` — no wheels needed)
# ─────────────────────────────────────────────────────────────
streamlit==1.62.0
earthengine-api==1.7.40
geemap==0.37.2
folium==0.20.0
streamlit-folium==0.27.4
plotly==6.9.0
sqlalchemy==2.0.52
geoalchemy2==0.20.0
psycopg2-binary==2.9.12
python-dotenv==1.2.3
pydantic-settings==2.15.0
