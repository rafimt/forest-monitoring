# Deployment (Streamlit Cloud) — VIEWER ONLY.
# The viewer (app/view.py) just reads PostGIS; it does NOT need geopandas,
# fiona, rasterio, or earthengine-api. Those are only for the local ingest
# script — see requirements-ingest.txt.
# Left unpinned so Streamlit Cloud's Python gets matching binary wheels.
streamlit
sqlalchemy
psycopg2-binary
pandas
plotly
folium
streamlit-folium
shapely
python-dotenv
